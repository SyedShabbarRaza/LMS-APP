import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ResourcesScreen from '@/src/screens/resources/resources.screen'

export default function index() {
  return (
    <ResourcesScreen/>
  )
}

const styles = StyleSheet.create({})